import { useMemo, useState } from 'react';

import { buildSchema } from './generator/parser';
import { downloadZip, buildExportPackage, exportPackageText } from './export/package';
import { GeneratedRenderer } from './renderers/GeneratedRenderer';
import { applySafeRepairs, evaluateQuality } from './generator/quality';
import { Chip, Icons, Section, StyleControls, title, widths, WorkbenchHeader, type Template } from './components/AppSections';
import type { Tokens, Viewport } from './types/schema';

const defaultPrompt = 'Create a sleek and modern pricing 4 card pricing display with $79/month $129/month $189/month and $229/month pricing cards for a local-first UI component generator called InterfaceForge with neural glow, export-ready code, and a launch CTA. Make the $229/month pricing card stand out by increasing size 20% and adding a subtle glow and 3d bevel effect.';
const templates: Template[] = [
  ['Neural pricing suite','Pricing',defaultPrompt],
  ['AI onboarding flow','Custom','Create a custom AI onboarding widget called OrbitFlow with three steps, circular progress rings, animated success state, invite teammates CTA, soft glass cards, identity setup, workspace preferences, import data step, and subtle cyan glow. Make the final success card stand out with a raised 3d bevel and celebratory highlight.'],
  ['Security command center','Settings','Design a premium security settings console called VaultOS with sidebar navigation, private mode toggle, biometric login, API key rotation, audit logs, device sessions, compliance status cards, warning states, and a save security settings CTA. Use a dark enterprise style with emerald highlights and subtle glow around critical controls.'],
  ['Founder KPI cockpit','Dashboard','Build a dense founder command dashboard called PulseRoom with revenue, burn multiple, runway, active users, conversion rate, growth alerts, investor-ready status feed, priority tasks, and a live signal strip. Make it feel like a futuristic operator cockpit with compact cards, luminous trend indicators, and export report CTA.'],
  ['Luxury checkout ritual','Checkout','Create a luxury checkout summary for an editorial fragrance brand called Maison Vale with cart line items, gift wrap, shipping method, loyalty credit, taxes, delivery promise, discount code, confirmation card, and complete purchase CTA.'],
  ['Support inbox console','Chat','Design a support inbox chat console called HelpDock with new assigned escalated queues, customer conversation thread, urgency badges, AI suggested reply, internal note field, resolved status, and send reply CTA.'],
  ['Booking concierge','Calendar','Create a premium booking scheduler for a design advisory studio called Northstar Sessions with available time slots, timezone selector, meeting length options, host profile, preparation checklist, confirmation CTA, and elegant editorial styling.']
];
const defaultTokens: Tokens = { text:'oklch(0.96 0.01 240)', muted:'oklch(0.76 0.03 240)', button:'oklch(0.68 0.15 225)', highlight:'oklch(0.72 0.16 220)', cardBg:'oklch(0.20 0.02 255 / .74)', cardBorder:'oklch(0.58 0.07 235 / .34)', radius:24, buttonRadius:999, fontScale:1 };

export default function App(){const[draftPrompt,setDraftPrompt]=useState(defaultPrompt); const[generatedPrompt,setGeneratedPrompt]=useState(defaultPrompt); const[design,setDesign]=useState(defaultTokens); const[viewport,setViewport]=useState<Viewport>('fluid'); const[copied,setCopied]=useState(false); const[active,setActive]=useState(templates[0][0]); const draftSchema=useMemo(()=>buildSchema(draftPrompt),[draftPrompt]); const[overrideSchema,setOverrideSchema]=useState<ReturnType<typeof buildSchema>|null>(null); const generatedSchema=useMemo(()=>buildSchema(generatedPrompt),[generatedPrompt]); const schema=overrideSchema??generatedSchema; const quality=useMemo(()=>evaluateQuality(schema),[schema]); const exportPkg=useMemo(()=>buildExportPackage(schema,design),[schema,design]); const artifact=exportPackageText(exportPkg); const copy=async()=>{await navigator.clipboard.writeText(artifact); setCopied(true); setTimeout(()=>setCopied(false),1000)}; const {Copy,Download,Monitor,Smartphone,Tablet}=Icons; return <main className="app" style={{'--accent':design.highlight} as React.CSSProperties}><WorkbenchHeader schema={schema} draftPrompt={draftPrompt} generatedPrompt={generatedPrompt} onGenerate={()=>{setGeneratedPrompt(draftPrompt);setOverrideSchema(null);}}/><section className="workspace"><aside className="rail"><Section title="Describe" n="01"/><textarea value={draftPrompt} onChange={e=>setDraftPrompt(e.target.value)}/><div className="chips"><Chip label="Pattern" value={draftSchema.pattern}/><Chip label="Strategy" value={draftSchema.strategy}/><Chip label="Data" value={draftSchema.plans.length?`${draftSchema.plans.length} plans`:`${draftSchema.requirements.length} reqs`}/></div><Section title="Showcase prompts" n="02"/><div className="templates">{templates.map(t=><div key={t[0]} className="template-item"><button onClick={()=>{setDraftPrompt(t[2]);setActive(t[0])}} className={active===t[0]?'selected':''}><strong>{t[0]}</strong><span>{t[1]}</span><small>{t[2]}</small></button><button onClick={()=>{setDraftPrompt(t[2]);setGeneratedPrompt(t[2]);setActive(t[0]);}} className="ghost">Use + Generate</button></div>)}</div><Section title="Style" n="03"/><StyleControls design={design} setDesign={setDesign}/></aside><section className="canvas"><header className="canvas-head"><div><p>Assembly canvas</p><h2>{schema.pattern==='pricing'?'Pricing System':title(schema.pattern)}</h2><span>{schema.subhead}</span></div><div className="viewports"><button onClick={()=>setViewport('mobile')}><Smartphone size={14}/></button><button onClick={()=>setViewport('tablet')}><Tablet size={14}/></button><button onClick={()=>setViewport('desktop')}><Monitor size={14}/></button><button onClick={()=>setViewport('fluid')}>Auto</button></div></header><div className="stage"><div style={{width:widths[viewport],maxWidth:'100%'}}><GeneratedRenderer schema={schema} design={design} viewport={viewport}/></div></div></section><aside className="inspector"><Section title="Critic" n="04"/><div className="score"><strong>{quality.overallScore}</strong><span>Deterministic quality score</span></div><div className="chip"><small>Prompt coverage</small><b>{quality.promptCoverageScore}</b></div><div className="chip"><small>Directive execution</small><b>{quality.directiveExecutionScore}</b></div><div className="chip"><small>Interaction readiness</small><b>{quality.interactionScore}</b></div><div className="chip"><small>Export readiness</small><b>{quality.exportReadinessScore}</b></div><button onClick={()=>setOverrideSchema(applySafeRepairs(schema,quality))}>Apply safe repairs</button><div className="templates">{quality.issues.slice(0,6).map((issue,i)=><div className="chip" key={`issue-${i}`}><small>{issue.type}</small><b>{issue.message}</b></div>)}</div><div className="templates">{quality.suggestedRepairs.map((repair,i)=><div className="chip" key={`repair-${i}`}><small>{repair.type}</small><b>{repair.reason}</b></div>)}</div><div className="templates">{schema.requirements.map((r,i)=><div className="chip" key={`${r.label}-${i}`}><small>{r.bucket} · {r.status}</small><b>{r.label}</b></div>)}</div><Section title="Export" n="05"/><button onClick={copy}><Copy size={16}/>{copied?'Copied':'Copy export package'}</button><button onClick={()=>downloadZip(`${schema.product.toLowerCase()}-export.zip`,exportPkg)}><Download size={16}/>Download ZIP package</button><div className="file-list"><span>schema.json</span><span>design-tokens.json</span><span>GeneratedComponent.ts</span><span>README.md</span></div></aside></section></main>}
